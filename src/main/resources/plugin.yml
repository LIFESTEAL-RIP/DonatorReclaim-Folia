name: DonatorReclaim
version: ${project.version}
main: nl.chimpgamer.donatorreclaim.DonatorReclaim
authors: [ChimpGamer]
folia-supported: true

commands:
  reclaim:
    description: Reclaim your rewards as donator.
    permission: donatorreclaim.commands.reclaim